"use client";
import { zodResolver } from "@hookform/resolvers/zod";
import { Loader2, Send } from "lucide-react";
import { useForm } from "react-hook-form";
import { toast } from "sonner";
import { leadSchema, type LeadInput } from "@/lib/validation";
export function LeadForm() { const { register, handleSubmit, reset, formState: { errors, isSubmitting } } = useForm<LeadInput>({ resolver: zodResolver(leadSchema) }); const submit = async (values: LeadInput) => { try { const response = await fetch("/api/leads", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(values) }); if (!response.ok) throw new Error(); reset(); toast.success("Thanks — your lead is in the pipeline."); } catch { toast.error("Something went wrong. Please try again."); } }; const error = (name: keyof LeadInput) => errors[name]?.message; return <form onSubmit={handleSubmit(submit)} className="glass rounded-2xl p-5 shadow-2xl sm:p-7"><div className="grid gap-4 sm:grid-cols-2"><Field label="Full name" error={error("fullName")}><input {...register("fullName")} placeholder="Alex Morgan" /></Field><Field label="Email" error={error("email")}><input {...register("email")} type="email" placeholder="alex@company.com" /></Field></div><Field label="Budget range" error={error("budget")}><select {...register("budget")} defaultValue=""><option value="" disabled>Choose a range</option><option>Under $5,000</option><option>$5,000 – $15,000</option><option>$15,000 – $50,000</option><option>$50,000+</option></select></Field><Field label="Tell us about your goals" error={error("message")}><textarea {...register("message")} rows={4} placeholder="What would you like to accomplish?" /></Field><button disabled={isSubmitting} className="btn-primary mt-2 w-full disabled:cursor-not-allowed disabled:opacity-70">{isSubmitting ? <Loader2 className="mr-2 h-4 w-4 animate-spin" /> : <Send className="mr-2 h-4 w-4" />}{isSubmitting ? "Sending…" : "Start the conversation"}</button></form>; }
function Field({ label, error, children }: { label: string; error?: string; children: React.ReactNode }) { return <label className="mb-4 block text-sm font-medium text-slate-200">{label}<span className="mt-1 block [&_input]:w-full [&_input]:rounded-xl [&_input]:border [&_input]:border-white/10 [&_input]:bg-white/5 [&_input]:px-3 [&_input]:py-3 [&_input]:text-sm [&_input]:outline-none [&_input]:transition [&_input]:focus:border-brand-400 [&_select]:w-full [&_select]:rounded-xl [&_select]:border [&_select]:border-white/10 [&_select]:bg-[#151929] [&_select]:px-3 [&_select]:py-3 [&_select]:text-sm [&_select]:outline-none [&_select]:focus:border-brand-400 [&_textarea]:w-full [&_textarea]:resize-none [&_textarea]:rounded-xl [&_textarea]:border [&_textarea]:border-white/10 [&_textarea]:bg-white/5 [&_textarea]:px-3 [&_textarea]:py-3 [&_textarea]:text-sm [&_textarea]:outline-none [&_textarea]:focus:border-brand-400">{children}</span>{error && <span className="mt-1 block text-xs text-rose-400">{error}</span>}</label> }
